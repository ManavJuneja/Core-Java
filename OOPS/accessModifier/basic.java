package OOPS.accessModifier;

public class basic {
    //Keyword ha jo define karta ha visibility classes, variable, methods ki
    /*
Modifier	Within  Same Class	Within Same Package	Subclass (Different Package)	Outside Package (Non-Subclass)
private	    ✅ Yes	❌ No	    ❌ No	                                            ❌ No
default 	✅ Yes	✅ Yes	    ❌ No	                                            ❌ No
protected	✅ Yes	✅ Yes	    ✅ Yes	                                            ❌ No
public	    ✅ Yes	✅ Yes	    ✅ Yes	                                            ✅ Yes

private → Only accessible within the same class.

default (package-private) → Accessible only within the same package (but not in subclasses outside the package).

protected → Accessible within the same package AND also in subclasses outside the package.

public → Accessible everywhere (inside and outside the package, subclass or not).



     */
}
